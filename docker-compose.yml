name: digitalplat-register

services:
  register:
    image: ${IMAGE:?Set IMAGE in .env}
    platform: linux/amd64
    pull_policy: always
    init: true
    shm_size: 1gb
    restart: unless-stopped
    ports:
      - "8400:8400"
    environment:
      REFERRAL_CODE: ${REFERRAL_CODE:-4qn8iw8r1o}
      JOBS_PATH: /app/data/jobs.json
      TURNSTILE_SOLVER_TYPE: remote
      TURNSTILE_REMOTE_ENDPOINT: ${TURNSTILE_REMOTE_ENDPOINT:?Set TURNSTILE_REMOTE_ENDPOINT in .env}
      TURNSTILE_TIMEOUT: ${TURNSTILE_TIMEOUT:-120}
      BROWSER_ENGINE: ${BROWSER_ENGINE:-camoufox}
      BROWSER_HEADLESS: "true"
      BROWSER_TIMEOUT: ${BROWSER_TIMEOUT:-90000}
      EMAIL_PROVIDER: mail.td
      EMAIL_TIMEOUT: ${EMAIL_TIMEOUT:-300}
      VERIFICATION_TIMEOUT: ${VERIFICATION_TIMEOUT:-300}
      VERIFICATION_CHECK_INTERVAL: ${VERIFICATION_CHECK_INTERVAL:-5}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      PROXY_ENABLED: ${PROXY_ENABLED:-false}
      PROXY_SERVER: ${PROXY_SERVER:-}
      PROXY_USERNAME: ${PROXY_USERNAME:-}
      PROXY_PASSWORD: ${PROXY_PASSWORD:-}
    command: ["digitalplat-register-web", "--host", "0.0.0.0", "--port", "8400"]
    volumes:
      - digitalplat-register-data:/app/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8400/health')"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  digitalplat-register-data:
